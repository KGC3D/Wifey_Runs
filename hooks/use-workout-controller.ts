// Workout state machine hook. Implementation lands in PR2 (Phase 2).
// Will own: status, startedAtMs, pauseStartedAtMs, pausedAccumulatedMs,
// nowMs, lastSpokenStepIndex. Drives speech on step transitions.

export {};
