// Pure timer math. Implementation lands in PR2 (Phase 2).
// Will export functions that derive currentStepIndex / elapsedMs / remainingMs
// from raw timestamps (startedAtMs, pausedAccumulatedMs, nowMs) without relying
// on interval-tick counts.

export {};
