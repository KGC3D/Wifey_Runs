// Web Speech API wrapper. Implementation lands in PR2 (Phase 3).
// Will provide: voice loading via voiceschanged, female-voice heuristic,
// speak(label) that cancels stale queued utterances, and a graceful no-op
// fallback when speechSynthesis is unavailable.

export {};
